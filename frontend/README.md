# Frontend

This frontend is part of the `Gemini API Powered Intelligent Care Assistant` project.

For full setup, architecture, API endpoints, and progress status, see:
- `../README.md`

## Quick Start

```bash
npm install
npm run dev
```

The app runs on `http://localhost:8080`.
